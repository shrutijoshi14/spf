import '../buttons.css';

const Buttons = () => {
  return (
    <div className="quick-actions">
      <button className="action-btn primary">👤 + Add Borrower</button>
      <button className="action-btn secondary">💰 + Add Loan</button>
    </div>
  );
};

export default Buttons;
