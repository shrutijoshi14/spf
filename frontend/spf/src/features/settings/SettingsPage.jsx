const SettingsPage = () => {
  return (
    <div className="page">
      <h2>Settings</h2>
      Update your profile, manage account settings, control user permissions, and adjust security
      options.
    </div>
  );
};

export default SettingsPage;
